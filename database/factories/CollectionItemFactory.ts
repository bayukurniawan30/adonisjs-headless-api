import CollectionItem from 'App/Models/CollectionItem'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(CollectionItem, ({ faker }) => {
  return {
    //
  }
}).build()
