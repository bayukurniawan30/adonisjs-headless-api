import AvailableField from 'App/Models/AvailableField'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(AvailableField, ({ faker }) => {
  return {
    //
  }
}).build()
